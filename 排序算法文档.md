# 排序算法可视化 - 技术文档

## 目录结构

```
src/
├── components/
│   ├── ControlPanel.vue       # 控制面板（算法选择、速度、大小、按钮）
│   ├── SortBarCanvas.vue      # Canvas 渲染容器 + 步骤动画执行
│   ├── SortVisualizer.vue     # 算法组件动态切换 + 暴露控制方法
│   ├── algorithms/
│   │   ├── index.ts           # 导出所有算法组件
│   │   ├── BubbleSort.vue     # 冒泡排序
│   │   ├── InsertionSort.vue  # 插入排序
│   │   ├── MergeSort.vue      # 归并排序
│   │   └── QuickSort.vue      # 快速排序
│   └── ...
├── composables/
│   ├── useSortAnimation.ts    # 动画编排逻辑（步骤驱动、播放控制）
│   └── useCanvasRenderer.ts   # Canvas 渲染引擎（绘制、动画队列）
├── stores/
│   └── sortStore.ts           # Pinia 状态管理（数组、速度、算法选择）
├── types/
│   └── sorting.ts             # 类型定义（SortStep、SortAlgorithm）
├── utils/
│   └── sortingAlgorithms.ts   # 排序算法实现（预计算步骤）
├── styles/
│   └── shared.css             # 共享样式（stats-bar）
└── App.vue                    # 根组件 + 键盘快捷键
```

---

## 架构设计

### 核心设计思路：步骤预计算 + 异步动画同步

```
用户操作 → store.isPlaying 变化 → useSortAnimation 响应
    → 从预计算 steps 取当前步骤
    → 调用 canvasRef.applyStep(step) 驱动动画
    → await 动画完成后再推进下一步
```

**为什么这样做：**
1. **预计算步骤**：排序算法一次性生成所有步骤，存储于 `steps[]`，确保排序逻辑与渲染完全解耦
2. **异步动画同步**：通过 `async/await` 保证每一步动画播放完毕后才执行下一步，视觉上流畅且准确
3. **状态分层**：
   - `sortStore`：全局状态（originalArray、animationSpeed、arraySize、algorithm）
   - `useSortAnimation`：各算法组件私有状态（array、steps、currentStep、comparisons、swaps、isPlaying）
   - `useCanvasRenderer`：纯渲染状态（barStates、animationQueue）

### 组件层次

```
App.vue
├── ControlPanel.vue           # 操作入口（emit reset/step/new-array）
└── SortVisualizer.vue         # 动态组件切换
    └── [BubbleSort|InsertionSort|MergeSort|QuickSort]
        ├── SortBarCanvas.vue  # Canvas 渲染 + 动画执行
        └── stats-bar          # 统计信息（stats-left: 按钮+状态 | operation-item: 当前操作）
```

**stats-bar 布局**：
- `stats-left`：控制按钮（播放/暂停、单步、重置）+ 紧凑统计（比较/交换/步骤）+ 状态指示器
- `operation-item`（固定右侧）：当前操作描述

### 状态指示器

4状态圆点动画，通过 `statusClass` 和 `statusText` computed 映射：

| 状态 | class | 颜色 | 条件 |
|------|-------|------|------|
| 就绪 | `ready` | 灰色 `#8b95a8` | `currentStep === 0` |
| 播放中 | `playing` | 青色 `#5dddd4` + pulse 动画 | `isPlaying === true` |
| 已暂停 | `paused` | 红色 `#ff8a8a` | `!isPlaying && currentStep > 0 && currentStep < steps.length` |
| 已完成 | `done` | 绿色 `#6bff6b` | `currentStep >= steps.length` |

---

## 渲染机制

### Canvas 渲染循环

`useCanvasRenderer` 内部通过 `requestAnimationFrame` 驱动持续渲染循环：

```typescript
function startRenderLoop() {
  if (animationFrameId !== null) return;
  animationFrameId = requestAnimationFrame(draw); // 循环绘制
}
```

每帧执行：
1. `drawBackground()` — 绘制渐变背景 + 网格线
2. `processAnimations(timestamp)` — 更新动画队列中的柱状图位置
3. `barStates.forEach(drawBar)` — 绘制每根柱子

### 柱状图状态

```typescript
interface BarState {
  index: number;      // 在数组中的原始索引
  value: number;      // 数值（唯一标识，排序中不变）
  x, targetX: number; // 当前位置 & 目标位置
  y: number;          // y 坐标（动画时会上升形成弧线）
  width, height: number;
  color: { r, g, b }; // RGB 颜色
  glowIntensity: number; // 发光强度
  isAnimating: boolean;
}
```

### 高亮状态映射

通过 `highlightedIndices` computed 从当前步骤推导颜色：

| 状态 | 颜色 | 场景 |
|------|------|------|
| comparing | 黄色 `#FFCC00` | 正在比较的两个元素 |
| swapping | 红色 `#FF6B6B` | 正在交换的两个元素 |
| sorted | 绿色 `#00FFA0` | 已确定位置的元素 |
| pivot | 紫色 `#9B59B6` | 快速排序的基准元素 |

### 交换动画（swap / merge）

`animateSwap` 使用**抛物线弧度**模拟真实交换：

```typescript
const arcHeight = Math.sin(progress * Math.PI) * 50; // 0 → 最高 → 0
bar1.x = startX1 + deltaX * progress;
bar1.y = baseY - arcHeight; // 向上抛起
```

使用 `easeOutCubic` 缓动曲线使动画平滑。

---

## 控制方式

### 外部控制流

```
ControlPanel 按钮
    ↓ emit
App.vue 事件处理函数
    ↓ 调用
SortVisualizer 暴露方法（reset / generateArray / step）
    ↓ 调用
算法组件的 useSortAnimation 方法
```

### 播放控制逻辑

```typescript
// useSortAnimation.ts
watch(isPlaying, (playing) => playing ? play() : stop());

function play() { localPlaying.value = true; step(); }
async function step() {
  if (!localPlaying.value || currentStep.value >= steps.value.length) {
    localPlaying.value = false; return;
  }
  const delay = await applyStep(steps.value[currentStep.value]);
  timer = setTimeout(step, delay ?? speed.value);
}
```

`applyStep` 中 `await canvasRef.value?.applyStep(step)` 确保动画完成后才 `setTimeout(step, delay)` 进入下一步。

### 单步执行

`stepOnce()` 独立于播放循环，直接执行当前步骤：
```typescript
async function stepOnce() {
  if (!localPlaying.value && currentStep.value < steps.value.length)
    await applyStep(steps.value[currentStep.value]);
}
```

---

## 核心类型定义

```typescript
// types/sorting.ts
type StepType = "compare" | "swap" | "merge" | "set" | "sorted" | "pivot";

interface SortStep {
  type: StepType;         // 步骤类型
  indices: number[];      // 涉及的数组索引
  description: string;    // 步骤描述（显示在 stats-bar）
  arraySnapshot?: number[]; // 执行此步骤后的数组快照（用于 swap/merge 动画定位）
}

type SortAlgorithm = "bubble" | "insertion" | "merge" | "quick";
```

---

## 排序算法详解

### 冒泡排序 (Bubble Sort)

**思想**：相邻元素两两比较，大值不断"冒泡"到数组末端。每轮遍历将当前最大值移动到正确位置。

**核心代码**：
```typescript
// sortingAlgorithms.ts
export function bubbleSort(arr: number[]): SortStep[] {
  const steps: SortStep[] = [];
  const n = arr.length;
  const array = [...arr];

  for (let i = 0; i < n - 1; i++) {           // 外循环：轮数
    for (let j = 0; j < n - i - 1; j++) {     // 内循环：相邻比较
      steps.push(createStep("compare", [j, j + 1],
        `比较 arr[${j}]=${array[j]} 和 arr[${j + 1}]=${array[j + 1]}`));

      if (array[j] > array[j + 1]) {           // 逆序则交换
        steps.push(createStep("swap", [j, j + 1],
          `交换 arr[${j}] 和 arr[${j + 1}]`, [...array]));
        [array[j], array[j + 1]] = [array[j + 1], array[j]];
      }
    }
    steps.push(createStep("sorted", [n - i - 1], // 标记已排序
      `arr[${n - i - 1}]=${array[n - i - 1]} 已排序`, [...array]));
  }
  return steps;
}
```

**示例**：`[5, 3, 8, 1, 9, 4, 7, 2, 6]`
- 第 1 轮：`[3, 5, 1, 2, 6, 4, 7, 8, 9]` — 9 冒泡到末端
- 第 2 轮：`[3, 1, 2, 5, 6, 4, 7, 8, 9]` — 8 就位
- 第 3 轮：`[1, 2, 3, 4, 5, 6, 7, 8, 9]` — 7 就位
- 依此类推... 直至全部有序

**复杂度**：O(n²) 时间，O(1) 空间

---

### 插入排序 (Insertion Sort)

**思想**：将数组分为"有序区"和"无序区"，逐个将无序区元素插入有序区的正确位置，像整理扑克牌。

**核心代码**：
```typescript
export function insertionSort(arr: number[]): SortStep[] {
  const steps: SortStep[] = [];
  const array = [...arr];
  const n = arr.length;

  // 初始：第一个元素作为有序区起点
  steps.push(createStep("sorted", [0],
    `arr[0]=${array[0]} 已排序（作为初始有序区）`, [...array]));

  for (let i = 1; i < n; i++) {
    const key = array[i];
    let j = i - 1;

    steps.push(createStep("compare", [i],
      `将 arr[${i}]=${key} 插入有序区`, [...array]));

    // 从有序区末尾向左比较，大于 key 则后移
    while (j >= 0 && array[j] > key) {
      steps.push(createStep("compare", [j, j + 1],
        `比较 arr[${j}]=${array[j]} 和 key=${key}`, [...array]));
      steps.push(createStep("set", [j + 1],
        `将 arr[${j}]=${array[j]} 后移到 arr[${j + 1}]`, [...array]));
      array[j + 1] = array[j];
      j--;
    }
    array[j + 1] = key;
    steps.push(createStep("set", [j + 1],
      `将 key=${key} 插入到 arr[${j + 1}]`, [...array]));
  }
  return steps;
}
```

**示例**：`[5, 3, 8, 1, 9, 4, 7, 2, 6]`
- 有序区 `[5]`，处理 3：向后移动 5，插入 3 → `[3, 5, 8, 1, 9, 4, 7, 2, 6]`
- 处理 8：在 5 后，无需移动 → `[3, 5, 8, 1, 9, 4, 7, 2, 6]`
- 处理 1：移动 8、5、3，插入 1 → `[1, 3, 5, 8, 9, 4, 7, 2, 6]`
- 处理 9：在 8 后，无需移动 → `[1, 3, 5, 8, 9, 4, 7, 2, 6]`
- 处理 4：移动 8、5、3，插入 4 → `[1, 3, 4, 5, 8, 9, 7, 2, 6]`
- 依此类推... 直至 `[1, 2, 3, 4, 5, 6, 7, 8, 9]`

**复杂度**：O(n²) 时间，O(1) 空间

---

### 归并排序 (Merge Sort)

**思想**：分治策略。将数组递归分割为单个元素，再自底向上合并有序子数组。合并时两路归并。

**核心代码**：
```typescript
export function mergeSort(arr: number[]): SortStep[] {
  const steps: SortStep[] = [];
  const array = [...arr];

  function merge(left: number, mid: number, right: number) {
    const leftArr = array.slice(left, mid + 1);
    const rightArr = array.slice(mid + 1, right + 1);
    let i = 0, j = 0, k = left;

    steps.push(createStep("compare", [left, right],
      `合并区间 [${left}, ${mid}] 和 [${mid + 1}, ${right}]`, [...array]));

    // 两路归并
    while (i < leftArr.length && j < rightArr.length) {
      steps.push(createStep("compare", [left + i, mid + 1 + j],
        `比较 leftArr[${i}]=${leftArr[i]} 和 rightArr[${j}]=${rightArr[j]}`,
        [...array]));

      if (leftArr[i] <= rightArr[j]) {
        steps.push(createStep("set", [k], `将 ${leftArr[i]} 放到位置 ${k}`, [...array]));
        array[k] = leftArr[i++];
      } else {
        steps.push(createStep("set", [k], `将 ${rightArr[j]} 放到位置 ${k}`, [...array]));
        array[k] = rightArr[j++];
      }
      k++;
    }
    // 复制剩余元素
    while (i < leftArr.length) { /* ... set remaining leftArr ... */ }
    while (j < rightArr.length) { /* ... set remaining rightArr ... */ }

    steps.push(createStep("merge",
      Array.from({ length: right - left + 1 }, (_, idx) => left + idx),
      `区间 [${left}, ${right}] 合并完成`, [...array]));
  }

  function sort(left: number, right: number) {
    if (left >= right) return;
    const mid = Math.floor((left + right) / 2);
    sort(left, mid);
    sort(mid + 1, right);
    merge(left, mid, right);
  }

  sort(0, array.length - 1);
  return steps;
}
```

**示例**：`[5, 3, 8, 1, 9, 4, 7, 2, 6]`
- 分割：`[5, 3, 8, 1]` `[9, 4, 7, 2, 6]`
- 再分：`[5, 3]` `[8, 1]` `[9, 4]` `[7, 2, 6]`
- 细分为：`[5]` `[3]` `[8]` `[1]` `[9]` `[4]` `[7]` `[2]` `[6]`
- 合并：`[3, 5]` `[1, 8]` `[4, 9]` `[2, 6, 7]`
- 继续归并：`[1, 3, 5, 8]` `[2, 4, 6, 7, 9]`
- 最终：`[1, 2, 3, 4, 5, 6, 7, 8, 9]`

**复杂度**：O(n log n) 时间，O(n) 空间

---

### 快速排序 (Quick Sort)

**思想**：分治策略。选择基准（pivot），将数组分为小于基准和大于基准两部分，递归排序两部分。

**核心代码**：
```typescript
export function quickSort(arr: number[]): SortStep[] {
  const steps: SortStep[] = [];
  const array = [...arr];

  function partition(low: number, high: number): number {
    const pivot = array[high];
    steps.push(createStep("pivot", [high],
      `选择 arr[${high}]=${pivot} 作为基准`, [...array]));

    let i = low - 1;
    for (let j = low; j < high; j++) {
      steps.push(createStep("compare", [j, high],
        `比较 arr[${j}]=${array[j]} 和 pivot=${pivot}`, [...array]));

      if (array[j] < pivot) {
        i++;
        if (i !== j) {
          steps.push(createStep("swap", [i, j],
            `交换 arr[${i}]=${array[i]} 和 arr[${j}]=${array[j]}`, [...array]));
          [array[i], array[j]] = [array[j], array[i]];
        }
      }
    }
    // 将基准放到正确位置
    if (i + 1 !== high) {
      steps.push(createStep("swap", [i + 1, high],
        `将基准放到正确位置 arr[${i + 1}]=${array[i + 1]}`, [...array]));
      [array[i + 1], array[high]] = [array[high], array[i + 1]];
    }
    steps.push(createStep("sorted", [i + 1],
      `arr[${i + 1}]=${array[i + 1]} 已在正确位置`, [...array]));
    return i + 1;
  }

  function sort(low: number, high: number) {
    if (low >= high) {
      if (low === high)
        steps.push(createStep("sorted", [low],
          `arr[${low}]=${array[low]} 已排序`, [...array]));
      return;
    }
    const pi = partition(low, high);
    sort(low, pi - 1);
    sort(pi + 1, high);
  }

  sort(0, array.length - 1);
  return steps;
}
```

**示例**：`[5, 3, 8, 1, 9, 4, 7, 2, 6]`，选 `6` 为基准
- 基准 6：整理后 → `[5, 3, 4, 2, 6, 8, 9, 7, 1]`（6 到正确位置）
- 左 `[5, 3, 4, 2]` 选 `2` → `[2, 3, 4, 5]`（2 到正确位置）
- 右 `[8, 9, 7, 1]` 选 `1` → `[1, 7, 8, 9]`（1 到正确位置）
- 递归继续... 直至 `[1, 2, 3, 4, 5, 6, 7, 8, 9]`

**复杂度**：平均 O(n log n)，最坏 O(n²)，O(log n) 空间

---

## 算法对比

| 算法 | 时间复杂度 | 空间复杂度 | 稳定性 | 适用场景 |
|------|-----------|-----------|--------|---------|
| 冒泡 | O(n²) | O(1) | 稳定 | 教学、小规模数据 |
| 插入 | O(n²) | O(1) | 稳定 | 基本有序、数据较少 |
| 归并 | O(n log n) | O(n) | 稳定 | 大数据、外部排序 |
| 快速 | O(n log n) | O(log n) | 不稳定 | 通用场景、高性能 |

---

## 共享可组合函数

### useSortAnimation

`src/composables/useSortAnimation.ts`

**职责**：动画编排 — 管理步骤序列、播放状态、统计计数、单步执行、重置

**API**：
```typescript
const {
  array,              // 当前显示数组
  steps,              // 所有预计算步骤
  currentStep,        // 当前步骤索引（0=未开始）
  comparisons,        // 比较次数
  swaps,              // 交换/合并次数
  highlightedIndices,  // 高亮索引 computed
  currentStepInfo,    // 当前步骤详情 computed
  isPlaying,          // 播放状态
  play,               // 开始/继续播放
  pause,              // 暂停播放
  step,               // 单步执行
  reset,              // 重置到初始状态
} = useSortAnimation({ sortFn, speed, canvasRef, originalArray });
```

**内部状态**：
- `localPlaying`：内部播放状态（独立于 isPlaying，允许单步执行）
- `sortedIndices`：`Set<number>` 已排序索引集合（增量维护）
- `valueToDisplayIndices`：`Map<number, number[]>` 缓存 value→displayIndex 映射（处理重复值）

### useCanvasRenderer

`src/composables/useCanvasRenderer.ts`

**职责**：Canvas 渲染 — 初始化、更新柱状态、驱动动画、渲染循环

**核心方法**：
```typescript
{
  barStates,         // 柱状图状态数组
  initialize,        // 初始化尺寸
  resize,            // 响应容器尺寸变化
  updateBars,        // 根据数组重建柱状态
  startRenderLoop,   // 启动 requestAnimationFrame 循环
  stopRenderLoop,    // 停止渲染循环
  onStep,            // 执行排序步骤（swap/merge 驱动动画）
  queueSwap          // 将交换动画加入队列
}
```

---

## Pinia Store

`src/stores/sortStore.ts`

全局状态（播放控制已移至 `useSortAnimation`）：

```typescript
{
  originalArray,      // 原始数组（重置时复制）
  animationSpeed,     // 动画速度 (ms)
  arraySize,          // 数组大小
  algorithm,          // 当前算法
  generateArray,      // 生成随机数组
  setSpeed,           // 设置速度
  setAlgorithm        // 设置算法
}
```

**注意**：`isPlaying`、`isComplete`、`startSort`、`pauseAnimation`、`stopAnimation` 已移除，
现由 `useSortAnimation` 内部管理。

### ArrayElement 类型

```typescript
interface ArrayElement {
  value: number;        // 数值
  displayIndex: number;  // 固定序号（1-based），随元素移动，用于显示
}
```
